/** Synthetic id for the always-present "whole organization" aggregate row used
 * across the Potential, Transparency, and Hierarchy pages — never a real
 * hierarchy_nodes id, so it can't collide with one. */
export const WHOLE_ORG_ID = "__whole_org__";
