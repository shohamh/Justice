import { useState } from "react";
import { EventDetailModal } from "../planning";
interface Props { open: boolean; count: number; onClose: () => void; onConfirm: (reason: string) => Promise<void>; }
export default function RangeBulkCancelDialog({ open, count, onClose, onConfirm }: Props) { const [reason,setReason]=useState(""); const [pending,setPending]=useState(false); async function submit(){ if(!reason.trim()||pending)return; setPending(true); try{await onConfirm(reason.trim());setReason("");onClose();}finally{setPending(false);} } return <EventDetailModal open={open} title={`ביטול ${count} מטווחים`} onClose={onClose}><div className="space-y-3"><label className="block text-sm">סיבת הביטול<textarea aria-label="סיבת הביטול" value={reason} onChange={e=>setReason(e.target.value)} className="mt-1 w-full rounded border p-2 text-gray-900 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100" /></label><button data-testid="confirm-bulk-cancel-button" disabled={!reason.trim()||pending} onClick={submit} className="rounded bg-red-600 px-4 py-2 text-white">אשר ביטול</button></div></EventDetailModal>; }
