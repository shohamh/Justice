// Change variant to switch badge color, or set to null to remove the badge entirely.
// Variants: "alpha" | "beta" | "stable" | "staging" | "dev" | "prod"
export const APP_ENV: { label: string; variant: "alpha" | "beta" | "stable" | "staging" | "dev" | "prod" } | null = {
  label: "alpha",
  variant: "alpha",
};
