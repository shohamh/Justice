# UI Issues #46-50 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix 5 UI issues: DutyTypeFormModal redesign+help, cancel button label, shift status badge, algorithm run badges, and score adjustment page in planning board.

**Architecture:** Pure frontend changes (Tasks 1-4) plus a new planning page (Task 5). No new backend API — score adjustments API already exists. All Hebrew text goes through he.json i18n keys.

**Tech Stack:** React 18 + TypeScript, Tailwind CSS, i18next (he.json), existing API clients in `frontend/src/api/`

---

## File Map

| File | Action | Tasks |
|------|--------|-------|
| `frontend/src/components/DutyTypeFormModal.tsx` | Modify | #46 |
| `frontend/src/i18n/he.json` | Modify | #46, #47, #50 |
| `frontend/src/pages/ShiftsPage.tsx` | Modify | #48, #49 |
| `frontend/src/pages/planning/ScoreAdjustmentPage.tsx` | Create | #50 |
| `frontend/src/App.tsx` | Modify | #50 |
| `frontend/src/components/UnifiedNav.tsx` | Modify | #50 |

---

### Task 1 (#46): Fix DutyTypeFormModal — layout, labels, help modal

**Files:**
- Modify: `frontend/src/components/DutyTypeFormModal.tsx`
- Modify: `frontend/src/i18n/he.json`

**Context:** The modal currently has poor spacing and uses "פנימי"/"חיצוני" as labels. We need to rename them to "תורנות פנימית"/"תורנות חיצונית", improve layout, and add an inline help toggle explaining the distinction.

- [ ] **Step 1: Read current DutyTypeFormModal**

```bash
cat frontend/src/components/DutyTypeFormModal.tsx
```

- [ ] **Step 2: Read current he.json duty_config section**

Search for `is_external` entries in `frontend/src/i18n/he.json` to find lines to update.

- [ ] **Step 3: Update he.json duty_config i18n keys**

Find and update these keys in `frontend/src/i18n/he.json`:

```json
"is_external": "תורנות פנימית / חיצונית",
"is_external_internal": "תורנות פנימית",
"is_external_external": "תורנות חיצונית",
"is_external_help_title": "מה ההבדל?",
"is_external_help_text": "תורנות פנימית — תורנות שנוצרת ומתוכננת בתוך היחידה. תורנות חיצונית — תורנות שהגיעה ליחידה מגורם חיצוני (למשל פקודה)."
```

- [ ] **Step 4: Rewrite DutyTypeFormModal with improved layout and help toggle**

Replace the component body with the improved version. Key changes:
1. Wrap all form fields in `<div className="space-y-4">` for consistent spacing
2. Add `const [showHelp, setShowHelp] = useState(false);` state
3. Rename "סוג תורנות (פנימי/חיצוני)" label to use `t("duty_config.is_external")`
4. Add help toggle button next to the label
5. Render inline help box below the select when `showHelp` is true

The is_external field group should look like:

```tsx
<div>
  <div className="flex items-center gap-2 mb-1">
    <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
      {t("duty_config.is_external")}
    </label>
    <button
      type="button"
      onClick={() => setShowHelp((v) => !v)}
      className="text-xs text-blue-500 hover:underline"
    >
      {t("duty_config.is_external_help_title")}
    </button>
  </div>
  {showHelp && (
    <div className="mb-2 text-xs text-gray-500 dark:text-gray-400 bg-gray-50 dark:bg-gray-700 rounded p-2 border border-gray-200 dark:border-gray-600">
      {t("duty_config.is_external_help_text")}
    </div>
  )}
  <select
    value={form.is_external ? "external" : "internal"}
    onChange={(e) => setForm({ ...form, is_external: e.target.value === "external" })}
    className="block w-full border border-gray-300 dark:border-gray-600 rounded-lg p-2 text-sm dark:bg-gray-700 dark:text-gray-100"
  >
    <option value="internal">{t("duty_config.is_external_internal")}</option>
    <option value="external">{t("duty_config.is_external_external")}</option>
  </select>
</div>
```

All other fields should be wrapped similarly in `<div>` blocks with consistent label + input spacing using `space-y-4` on the container.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/DutyTypeFormModal.tsx frontend/src/i18n/he.json
git commit -m "fix(#46): redesign DutyTypeFormModal layout and add help toggle for internal/external"
```

---

### Task 2 (#47): Fix cancel button label in new shift modal

**Files:**
- Modify: `frontend/src/i18n/he.json`

**Context:** The cancel button in the new shift modal uses `t("shifts.cancel")` which is currently "בטל משמרת". It should just say "ביטול".

- [ ] **Step 1: Find and verify the current value**

```bash
grep -n "cancel" frontend/src/i18n/he.json
```

Expected to find: `"cancel": "בטל משמרת"` under the `shifts` section.

- [ ] **Step 2: Update he.json**

Change:
```json
"cancel": "בטל משמרת",
```
To:
```json
"cancel": "ביטול",
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/he.json
git commit -m "fix(#47): change shift cancel button label from 'בטל משמרת' to 'ביטול'"
```

---

### Task 3 (#48): Show "פעיל" status badge in shifts table

**Files:**
- Modify: `frontend/src/pages/ShiftsPage.tsx`

**Context:** The `shift_status` column (around line 363-369) currently only renders a grey badge when `status === "cancelled"` and returns nothing for active shifts. We need to render a green "פעיל" badge for active shifts.

`shifts.active` = "פעיל" already exists in he.json.

- [ ] **Step 1: Read the shift_status column definition**

```bash
grep -n "shift_status\|cancelled\|active" frontend/src/pages/ShiftsPage.tsx | head -30
```

- [ ] **Step 2: Update the status column cell renderer**

Find the column definition that currently looks like:
```tsx
cell: (s) => s.status === "cancelled"
  ? <span ...>{t("shifts.cancelled")}</span>
  : null,
```

Replace with:
```tsx
cell: (s) => s.status === "cancelled"
  ? (
    <span className="px-2 py-0.5 rounded text-xs font-medium bg-gray-200 dark:bg-gray-700 text-gray-500 dark:text-gray-400">
      {t("shifts.cancelled")}
    </span>
  )
  : (
    <span className="px-2 py-0.5 rounded text-xs font-medium bg-green-100 dark:bg-green-900 text-green-700 dark:text-green-300">
      {t("shifts.active")}
    </span>
  ),
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/ShiftsPage.tsx
git commit -m "fix(#48): show 'פעיל' badge in shift status column by default"
```

---

### Task 4 (#49): Algorithm run status badges in shifts board

**Files:**
- Modify: `frontend/src/pages/ShiftsPage.tsx`

**Context:** The shifts page has an algorithm panel. We need to show badges: how many jobs are currently running (pending/running), and how many finished but not published (status === "done").

The API: `listJobs(limit, offset)` from `frontend/src/api/algorithm.ts` returns `JobListOut` with items having `status: "pending" | "running" | "done" | "failed" | "published"`.

- [ ] **Step 1: Read ShiftsPage to understand component structure and where AlgorithmInlinePanel is rendered**

```bash
grep -n "AlgorithmInlinePanel\|listJobs\|algorithm" frontend/src/pages/ShiftsPage.tsx | head -40
```

Also read the relevant section around the algorithm panel.

- [ ] **Step 2: Add job status state and fetch**

Add to ShiftsPage (or the relevant component) imports and state:

```tsx
import { listJobs } from "../api/algorithm";
```

Add state:
```tsx
const [runningCount, setRunningCount] = useState(0);
const [doneUnpublishedCount, setDoneUnpublishedCount] = useState(0);
```

Add useEffect to fetch and compute:
```tsx
useEffect(() => {
  listJobs(50, 0).then((data) => {
    setRunningCount(data.items.filter((j) => j.status === "pending" || j.status === "running").length);
    setDoneUnpublishedCount(data.items.filter((j) => j.status === "done").length);
  }).catch(() => {});
}, []);
```

- [ ] **Step 3: Render badges near the algorithm panel**

Find where `AlgorithmInlinePanel` is rendered and add status chips above or below it:

```tsx
<div className="flex gap-2 mb-2 flex-wrap" dir="rtl">
  {runningCount > 0 && (
    <span className="px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 dark:bg-yellow-900 text-yellow-700 dark:text-yellow-300">
      {runningCount} ריצות פעילות
    </span>
  )}
  {doneUnpublishedCount > 0 && (
    <span className="px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 dark:bg-blue-900 text-blue-700 dark:text-blue-300">
      {doneUnpublishedCount} הושלמו, ממתינות לפרסום
    </span>
  )}
</div>
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/ShiftsPage.tsx
git commit -m "feat(#49): show algorithm run status badges (running, done-unpublished) in shifts board"
```

---

### Task 5 (#50): Score adjustment page in planning board

**Files:**
- Create: `frontend/src/pages/planning/ScoreAdjustmentPage.tsx`
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/components/UnifiedNav.tsx`
- Modify: `frontend/src/i18n/he.json`

**Context:** 
- `frontend/src/api/scoreAdjustments.ts` already has `createAdjustment({ soldier_id, delta, reason })` and `listAdjustments(soldier_id?)`.
- The page should warn it's exceptional, require off-system approval, and let commanders create adjustments.
- The page should list recent adjustments (confidential — show only to appropriate hierarchy; the page itself is under planning so already restricted to commanders).
- The adjustment appears in the soldier's duty history timeline automatically (backend already handles this).
- Need a soldier selector — use `listSoldiers()` from `frontend/src/api/soldiers.ts`.

- [ ] **Step 1: Add i18n keys for the new page**

Add under `"score_adjustment"` key in he.json:
```json
"score_adjustment": {
  "nav_label": "שינוי ניקוד ידני",
  "title": "שינוי ניקוד ידני",
  "warning_title": "פעולה חריגה",
  "warning_text": "שינוי ניקוד ידני הוא פעולה חריגה המיועדת למקרים נקודתיים בלבד. ביצוע הפעולה מצריך אישור מחוץ למערכת מהדרג המוסמך. יש לתעד את הסיבה בצורה מדויקת.",
  "soldier_label": "חייל",
  "delta_label": "שינוי ניקוד (חיובי / שלילי)",
  "reason_label": "סיבה (חסויה — גלויה לחייל ולהיררכיה הפיקודית בלבד)",
  "submit_btn": "אשר שינוי",
  "success_msg": "שינוי הניקוד נשמר בהצלחה",
  "recent_title": "שינויים אחרונים",
  "no_adjustments": "אין שינויים לתצוגה",
  "delta_col": "שינוי",
  "reason_col": "סיבה",
  "soldier_col": "חייל",
  "date_col": "תאריך"
}
```

- [ ] **Step 2: Create ScoreAdjustmentPage.tsx**

```tsx
import { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import Layout from "../../components/Layout";
import { createAdjustment, listAdjustments } from "../../api/scoreAdjustments";
import { listSoldiers } from "../../api/soldiers";

interface Soldier {
  id: string;
  full_name: string;
}

interface Adjustment {
  id: string;
  soldier_id: string;
  soldier_name?: string;
  delta: string;
  reason: string;
  created_at: string;
}

export default function ScoreAdjustmentPage() {
  const { t } = useTranslation();
  const [soldiers, setSoldiers] = useState<Soldier[]>([]);
  const [adjustments, setAdjustments] = useState<Adjustment[]>([]);
  const [soldierId, setSoldierId] = useState("");
  const [delta, setDelta] = useState("");
  const [reason, setReason] = useState("");
  const [submitting, setSubmitting] = useState(false);
  const [successMsg, setSuccessMsg] = useState("");
  const [error, setError] = useState("");

  useEffect(() => {
    listSoldiers().then((data) => setSoldiers(data)).catch(() => {});
    loadAdjustments();
  }, []);

  function loadAdjustments() {
    listAdjustments().then((data) => setAdjustments(data)).catch(() => {});
  }

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (!soldierId || !delta || !reason.trim()) return;
    const deltaNum = parseFloat(delta);
    if (isNaN(deltaNum)) return;
    setSubmitting(true);
    setError("");
    setSuccessMsg("");
    try {
      await createAdjustment({ soldier_id: soldierId, delta: deltaNum, reason: reason.trim() });
      setSuccessMsg(t("score_adjustment.success_msg"));
      setSoldierId("");
      setDelta("");
      setReason("");
      loadAdjustments();
    } catch (err: unknown) {
      const detail = (err as { response?: { data?: { detail?: string } } })?.response?.data?.detail;
      setError(detail ?? "שגיאה");
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <Layout>
      <div className="max-w-2xl mx-auto space-y-6 py-4" dir="rtl">
        <h2 className="text-xl font-semibold">{t("score_adjustment.title")}</h2>

        {/* Warning card */}
        <div className="bg-yellow-50 dark:bg-yellow-900/30 border border-yellow-300 dark:border-yellow-700 rounded-lg p-4">
          <p className="font-semibold text-yellow-800 dark:text-yellow-300 text-sm mb-1">
            {t("score_adjustment.warning_title")}
          </p>
          <p className="text-yellow-700 dark:text-yellow-400 text-sm">
            {t("score_adjustment.warning_text")}
          </p>
        </div>

        {/* Form */}
        <form onSubmit={handleSubmit} className="bg-white dark:bg-gray-800 rounded-lg shadow p-5 space-y-4">
          <div>
            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
              {t("score_adjustment.soldier_label")}
            </label>
            <select
              value={soldierId}
              onChange={(e) => setSoldierId(e.target.value)}
              required
              className="block w-full border border-gray-300 dark:border-gray-600 rounded-lg p-2 text-sm dark:bg-gray-700 dark:text-gray-100"
            >
              <option value="">— בחר חייל —</option>
              {soldiers.map((s) => (
                <option key={s.id} value={s.id}>{s.full_name}</option>
              ))}
            </select>
          </div>

          <div>
            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
              {t("score_adjustment.delta_label")}
            </label>
            <input
              type="number"
              step="0.01"
              value={delta}
              onChange={(e) => setDelta(e.target.value)}
              required
              placeholder="+1.5 / -2.0"
              className="block w-full border border-gray-300 dark:border-gray-600 rounded-lg p-2 text-sm dark:bg-gray-700 dark:text-gray-100"
              dir="ltr"
            />
          </div>

          <div>
            <label className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
              {t("score_adjustment.reason_label")}
            </label>
            <textarea
              value={reason}
              onChange={(e) => setReason(e.target.value)}
              required
              rows={3}
              className="block w-full border border-gray-300 dark:border-gray-600 rounded-lg p-2 text-sm dark:bg-gray-700 dark:text-gray-100 resize-none"
            />
          </div>

          {error && <p className="text-red-500 text-sm">{error}</p>}
          {successMsg && <p className="text-green-600 dark:text-green-400 text-sm font-medium">{successMsg}</p>}

          <button
            type="submit"
            disabled={submitting || !soldierId || !delta || !reason.trim()}
            className="px-4 py-2 text-sm bg-blue-600 text-white rounded-lg hover:bg-blue-700 disabled:opacity-50"
          >
            {t("score_adjustment.submit_btn")}
          </button>
        </form>

        {/* Recent adjustments */}
        <div className="bg-white dark:bg-gray-800 rounded-lg shadow p-5">
          <h3 className="font-medium text-sm mb-3">{t("score_adjustment.recent_title")}</h3>
          {adjustments.length === 0 ? (
            <p className="text-sm text-gray-500">{t("score_adjustment.no_adjustments")}</p>
          ) : (
            <table className="w-full text-sm">
              <thead>
                <tr className="text-gray-500 dark:text-gray-400 border-b dark:border-gray-600">
                  <th className="text-right pb-2 font-medium">{t("score_adjustment.date_col")}</th>
                  <th className="text-right pb-2 font-medium">{t("score_adjustment.soldier_col")}</th>
                  <th className="text-right pb-2 font-medium">{t("score_adjustment.delta_col")}</th>
                  <th className="text-right pb-2 font-medium">{t("score_adjustment.reason_col")}</th>
                </tr>
              </thead>
              <tbody>
                {adjustments.map((a) => {
                  const soldier = soldiers.find((s) => s.id === a.soldier_id);
                  return (
                    <tr key={a.id} className="border-b dark:border-gray-600 last:border-0">
                      <td className="py-2 text-xs">{a.created_at.slice(0, 10)}</td>
                      <td className="py-2">{soldier?.full_name ?? a.soldier_id.slice(0, 8)}</td>
                      <td className={`py-2 font-medium ${Number(a.delta) >= 0 ? "text-green-600 dark:text-green-400" : "text-red-600 dark:text-red-400"}`}>
                        {Number(a.delta) >= 0 ? "+" : ""}{Number(a.delta).toFixed(2)}
                      </td>
                      <td className="py-2 text-xs text-gray-600 dark:text-gray-400 max-w-xs truncate">{a.reason}</td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
          )}
        </div>
      </div>
    </Layout>
  );
}
```

- [ ] **Step 3: Add route to App.tsx**

Read `frontend/src/App.tsx` around lines 75-88 and add:
```tsx
import ScoreAdjustmentPage from "./pages/planning/ScoreAdjustmentPage";
// ...
<Route path="/planning/score-adjustments" element={<ScoreAdjustmentPage />} />
```
Place it alongside the other `/planning/*` routes.

- [ ] **Step 4: Add nav item to UnifiedNav.tsx**

Read `frontend/src/components/UnifiedNav.tsx` around lines 111-114. The `planningItems` array currently has 3 items. Add:
```tsx
{ label: t("score_adjustment.nav_label"), path: "/planning/score-adjustments" },
```

- [ ] **Step 5: Check scoreAdjustments API shape**

Read `frontend/src/api/scoreAdjustments.ts` to confirm `listAdjustments()` return type and `createAdjustment` parameter shape. Adjust types in ScoreAdjustmentPage.tsx if they differ.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/pages/planning/ScoreAdjustmentPage.tsx frontend/src/App.tsx frontend/src/components/UnifiedNav.tsx frontend/src/i18n/he.json
git commit -m "feat(#50): add score adjustment page to planning board with disclaimer and recent list"
```

---

## Self-Review

**Spec coverage:**
- #46: DutyTypeFormModal layout fix ✅, label rename ✅, help modal ✅
- #47: Cancel button label ✅
- #48: Active badge in shift status column ✅
- #49: Running and done-unpublished badges ✅
- #50: Exceptional action disclaimer ✅, form ✅, planning board page ✅, affects scoring metrics (already handled by backend) ✅, reason confidentiality (page is commander-only, shown in list for commanders) ✅

**Placeholder scan:** No TBDs. All code shown. All commands exact.

**Type consistency:**
- `Soldier.id` used consistently as `string`
- `listSoldiers()` — need to verify exact return type in Task 5 Step 5
- `listAdjustments()` / `createAdjustment()` — Step 5 explicitly verifies the API shape before trusting the inline types
