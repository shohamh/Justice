# Browser E2E coverage matrix

The default browser projects are Chrome at 1440x1000 and 390x844. Tests run serially against a dedicated seeded PostgreSQL database.

| Role | Journey | Critical assertion | Viewports | Tier | Spec |
| --- | --- | --- | --- | --- | --- |
| Soldier | Submit personal request | Created request remains visible after reload | Desktop/mobile | Smoke | `smoke/soldier_requests.spec.ts` |
| Soldier, commander, duty manager | Approve request | Each approval advances visible status | Desktop/mobile | Smoke | `smoke/approval_workflow.spec.ts` |
| Soldier, unauthorized soldier | Authorization boundary | Forbidden reviewer action does not mutate request | Desktop/mobile | Smoke | `smoke/authorization_boundaries.spec.ts` |
| Soldier | Core views | Home, requests, calendar, and refresh render without page errors | Desktop/mobile | Smoke | `smoke/regular_user_views.spec.ts` |
| Admin | Planning configuration | Configuration page renders real duty types/locations | Desktop/mobile | Full | `smoke/admin_configuration.spec.ts` |
| Admin | Hierarchy table | Filtering, sorting, and empty state are usable | Desktop/mobile | Full | `smoke/table_interactions.spec.ts` |
| Admin | Seeded navigation | Hierarchy, calendar, transparency render seeded data | Desktop/mobile | Smoke | `seed_views.spec.ts` |
| Duty manager, commander, soldiers, reserves | Multi-user duty problems | Algorithm and manual assignment plus exemption, Gimelim, absence, Hakpaza, reserve replacement, and visible commander problem state | Desktop | Full | `smoke/multi_user_duty_problems.spec.ts` |
| Soldiers, commander, duty manager | Duty swaps | Marketplace ask+free cover, board trade cover, take-free, and proactive offer-swap journeys; dual-role (commander/duty-manager) manager approval exercised independently per (side, approver_kind); notification bell click-through (including a case where the destination tab genuinely shows the row); post-finalize duty ownership verified on `/my-duties`. `take-free` is intentionally covered (it does have a UI path, via `OfferSwapModal`'s free radio); the calendar's "shift-modal claim" entry point is intentionally NOT covered — confirmed unreachable for a plain soldier (`GET /swaps/for-assignment/{id}` 403s for non-owners without `SWAP_APPROVE`, silently swallowed by `ShiftDetailPanel`, so a soldier can never see the "cover this swap" banner from the calendar), tracked as background task `task_b2cdd6e2` ("Fix swaps.for-assignment 403 breaking cover banner for soldiers"); see `smoke/swaps.spec.ts`'s seam-inventory header for the full write-up. | Desktop | Full | `smoke/swaps.spec.ts` |
| Duty manager, commander | Range scheduling, attendance, and excusal | Event creation + primary/reserve assignment via the verified candidate-checkbox testids; attendance marked on the seeded past event and confirmed via the read-only status text after refresh; self-excusal + duty-manager decision confirmed via the real post-approval effect (assignment deleted, reserve promoted into the primary slot, pending queue empties) rather than a nonexistent "approved" badge; qualification tab renders an eligibility warning. Self-excusal is driven as `commander` rather than a plain soldier — confirmed via `GET /ranges/{event_id}`'s authorization that a plain soldier assigned to a `dutyManager`-created event has no UI path to view it at all (no assignee exception on that route). Not currently tracked — see `smoke/ranges.spec.ts`'s seam-inventory header for the full write-up. | Desktop | Full | `smoke/ranges.spec.ts` |
| Duty manager, commander, transferred soldier | Hierarchy transfer requests | Soldier moved between two sibling teams via both the click-based "quick add" search and a real dnd-kit pointer-based drag-and-drop (down -> incremental moves -> up, clearing the `PointerSensor`'s 8px activation distance) onto the destination node — both routes proven to hit the identical `openTransferConfirmation`/`POST /api/hierarchy-transfers` flow; each creation approved and the soldier's new placement confirmed under the destination node's own `tree-soldiers-{nodeId}` container (and absence from the source's) after a refresh; a third creation is rejected with a required note, confirmed visible on the *transferred soldier's* own `/my-requests` page — not the submitting commander/duty-manager's, since `GET /me/hierarchy-transfers` is keyed by the transferred soldier, not the requester (a real behavioural correction, not a testid fix, found by reading the route directly) — and the soldier is confirmed to remain at their original node. Node-drag-to-move (`moveNode`, a separate immediate no-approval structural edit sharing the same tree UI) is intentionally NOT covered here — flagged as a follow-up gap, not silently implied as tested. | Desktop | Full | `smoke/hierarchy_transfers.spec.ts` |
| Admin, commander | Rank advancement | Admin edits a rank-advancement interval on `/admin/settings` and it persists after reload; a scoped commander (not admin/duty-manager — the only role for which the narrow `rank-correction-toggle` flow is even reachable, since `canManage` actors skip straight to the full-profile editor) manually corrects a soldier's rank and next-rank-date via `UnifiedSoldierModal`, confirmed after a real full-page reload via both the new rank text and the `next_rank_date_manual` badge. The third journey is a deliberate HYBRID test, labeled as such in the spec's own seam inventory: there is genuinely no UI or HTTP trigger for the promotion worker (`_promote_due_soldiers()` only runs from the 24h poll loop), so the spec (1) uses the real UI to set a soldier's `next_rank_date` to today — the actual precondition the worker queries on — (2) shells out via Node's `child_process.execSync` to a new one-shot ops script (`backend/app/scripts/run_rank_advancement_once.py`, also independently useful for admins) that runs the real production `_promote_due_soldiers()` once against the same database, then (3) reloads the real UI and asserts the soldier's rank actually advanced to the next ladder rung and the badge reverted to `next_rank_date_automatic` — proving the real worker function ran, not just that a script exited 0. | Desktop | Full | `smoke/rank_advancement.spec.ts` |
| Duty manager, commander, soldier | Personal constraint override across duty, algorithm, and range paths | Approved personal constraint submitted and dual-stage approved; duty-side manual override is documented as reachable only through `ShiftAssignModal`'s "Replace" flow in principle — its own precondition (a weapon-ineligible original assignee) is confirmed unreachable through any live UI path, and the standard bulk `ShiftEditAssignmentsModal` is confirmed to have no override-reason UI at all (save 409s with `override_reason_required`, surfaced only as a generic error) — both are real, pre-existing product gaps, not test defects, tracked together as background task `task_af3d0c50` ("ShiftEditAssignmentsModal has no override UI; ShiftAssignModal's Replace-flow trigger is unreachable"); CP-SAT auto-assign unconditionally hard-excludes the constrained soldier from the published result, verified non-vacuous via the real "export inputs" button proving they were in the solver's loaded pool with an overlapping approved constraint first; range-side manual override via `RangeEditAssignmentsModal` succeeds with an override reason and the soldier appears in the roster after reload; range auto-select is confirmed to be a soft conflict, not a hard exclusion — the constrained soldier can be included in the auto-selected set, and saving that selection still requires the same override-reason gate as a manual pick (a corrected finding — an initial "auto-select also hard-excludes" assumption did not hold and was fixed during implementation). A separate, deeper solver-settings plumbing bug was also found while investigating a weapon-ineligibility fixture: the `enforce_weapon_qualification` admin toggle never reaches a real algorithm run, tracked as background task `task_f58fff09`. | Desktop | Full | `smoke/personal_constraint_override.spec.ts` |

Retry-passing tests remain failures for triage; a retry is diagnostic evidence, not a clean pass.
